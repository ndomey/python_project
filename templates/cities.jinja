{% extends "base.jinja" %}
{% block content %}
<h2>{{country.name}}</h2>
    <nav class="cities_nav">
        {% for city in cities %}
            <section>
                <li class="listofcities"><a href="/countries/{{country.id}}/{{city.city_name}}"></a><b>{{city.city_name}}</b></a>
                {% if city.visited %}
                <span> &#9989;</span>
                {% endif %}
                <form action="/countries/{{country.id}}/{{city.id}}/update" method="POST">
                    <label for="checked-out">
                    <span><b>visited</b></span>
                    <input 
                        type="checkbox" 
                        id="checked-out" 
                        name="checked_out" 
                        value="true"
                    >
                    </label>
                    <input class="buttons" type="submit" value="Update">
                </form>
                </li>
                <form action="/countries/{{country.id}}/{{city.city_name}}/delete" method="POST">
                <input class="buttons" type="submit" value="delete" />
                </form>
            </section>
        {% endfor %}
    </nav>
<center>
<form action="/countries/{{country.id}}/new" method="POST">
    <label for="city_name"></label>
    <input type="text" name="city_name" id="city_name"/>       
    <input class="buttons" type="submit" value="Add City">
</form>
</center>
{% endblock %}